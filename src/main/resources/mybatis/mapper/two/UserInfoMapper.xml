<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.equality.springbootdemo2.mapper.two.UserInfoMapper">

	<resultMap type="com.equality.springbootdemo2.entity.two.UserInfo" id="userInfoMap">
		<id property="id"  column="id"/>
		<result property="name" column="name"/>
		<result property="password" column="password"/>		
	</resultMap>
	
	<select id="findById" parameterType="int" resultMap="userInfoMap">
		select * from user_info where id = #{id}
	</select>
	
	<select id="querylist" resultMap="userInfoMap">
		select * from user_info order by id
	</select>
	
	<insert id="add" parameterType="com.equality.springbootdemo2.entity.two.UserInfo">
		insert into user_info(id,name,password) values(#{id},#{name},#{password})
	</insert>


</mapper>